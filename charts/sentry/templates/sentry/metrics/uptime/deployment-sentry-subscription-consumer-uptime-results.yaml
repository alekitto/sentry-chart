{{- if .Values.sentry.uptimeResultsConsumer.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ template "sentry.fullname" . }}-uptime-results-consumer
  labels:
    app: sentry
    chart: "{{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}"
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
    app.kubernetes.io/managed-by: "Helm"
  {{- if .Values.asHook }}
  {{- /* Add the Helm annotations so that deployment after asHook from true to false works */}}
  annotations:
    meta.helm.sh/release-name: "{{ .Release.Name }}"
    meta.helm.sh/release-namespace: "{{ .Release.Namespace }}"
    "helm.sh/hook": "post-install,post-upgrade"
    "helm.sh/hook-weight": "10"
  {{- end }}
spec:
  revisionHistoryLimit: {{ .Values.revisionHistoryLimit }}
  selector:
    matchLabels:
        app: sentry
        release: "{{ .Release.Name }}"
        role: sentry-uptime-results-consumer
  replicas: {{ .Values.sentry.subscriptionConsumerEapSpans.replicas }}
  template:
    metadata:
      annotations:
        checksum/configYml: {{ .Values.config.configYml | toYaml | toString | sha256sum }}
        checksum/sentryConfPy: {{ .Values.config.sentryConfPy | sha256sum }}
        checksum/config.yaml: {{ include "sentry.config" . | sha256sum }}
        {{- if .Values.sentry.uptimeResultsConsumer.annotations }}
{{ toYaml .Values.sentry.uptimeResultsConsumer.annotations | indent 8 }}
        {{- end }}
      labels:
        app: sentry
        release: "{{ .Release.Name }}"
        role: sentry-uptime-results-consumer
        {{- if .Values.sentry.uptimeResultsConsumer.podLabels }}
{{ toYaml .Values.sentry.uptimeResultsConsumer.podLabels | indent 8 }}
        {{- end }}
    spec:
      affinity:
      {{- if .Values.sentry.uptimeResultsConsumer.affinity }}
{{ toYaml .Values.sentry.uptimeResultsConsumer.affinity | indent 8 }}
      {{- end }}
      {{- if .Values.sentry.uptimeResultsConsumer.nodeSelector }}
      nodeSelector:
{{ toYaml .Values.sentry.uptimeResultsConsumer.nodeSelector | indent 8 }}
      {{- else if .Values.global.nodeSelector }}
      nodeSelector:
{{ toYaml .Values.global.nodeSelector | indent 8 }}
      {{- end }}
      {{- if .Values.sentry.uptimeResultsConsumer.tolerations }}
      tolerations:
{{ toYaml .Values.sentry.uptimeResultsConsumer.tolerations | indent 8 }}
      {{- else if .Values.global.tolerations }}
      tolerations:
{{ toYaml .Values.global.tolerations | indent 8 }}
      {{- end }}
      {{- if .Values.sentry.uptimeResultsConsumer.topologySpreadConstraints }}
      topologySpreadConstraints:
{{ toYaml .Values.sentry.uptimeResultsConsumer.topologySpreadConstraints | indent 8 }}
      {{- end }}
      {{- if .Values.images.sentry.imagePullSecrets }}
      imagePullSecrets:
{{ toYaml .Values.images.sentry.imagePullSecrets | indent 8 }}
      {{- end }}
      {{- if .Values.sentry.uptimeResultsConsumer.securityContext }}
      securityContext:
{{ toYaml .Values.sentry.uptimeResultsConsumer.securityContext | indent 8 }}
      {{- end }}
      containers:
      - name: {{ .Chart.Name }}-uptime-results-consumer
        image: "{{ template "sentry.image" . }}"
        imagePullPolicy: {{ default "IfNotPresent" .Values.images.sentry.pullPolicy }}
        command: ["sentry"]
        args:
          - "run"
          - "consumer"
          - "uptime-results"
          {{- if .Values.sentry.uptimeResultsConsumer.autoOffsetReset }}
          - "--auto-offset-reset"
          - "{{ .Values.sentry.uptimeResultsConsumer.autoOffsetReset }}"
          {{- end }}
          {{- if .Values.sentry.uptimeResultsConsumer.noStrictOffsetReset }}
          - "--no-strict-offset-reset"
          {{- end }}
          - "--consumer-group"
          - "uptime-results"
          {{- if .Values.sentry.subscriptionConsumerEapSpans.livenessProbe.enabled }}
          - "--healthcheck-file-path"
          - "/tmp/health.txt"
          {{- end }}
        {{- if .Values.sentry.subscriptionConsumerEapSpans.livenessProbe.enabled }}
        livenessProbe:
          exec:
            command:
              - rm
              - /tmp/health.txt
          initialDelaySeconds: {{ .Values.sentry.subscriptionConsumerEapSpans.livenessProbe.initialDelaySeconds }}
          periodSeconds: {{ .Values.sentry.subscriptionConsumerEapSpans.livenessProbe.periodSeconds }}
        {{- end }}
        env:
{{ include "sentry.env" . | indent 8 }}
{{- if .Values.sentry.uptimeResultsConsumer.env }}
{{ toYaml .Values.sentry.uptimeResultsConsumer.env | indent 8 }}
{{- end }}
        volumeMounts:
        - mountPath: /etc/sentry
          name: config
          readOnly: true
        - mountPath: {{ .Values.filestore.filesystem.path }}
          name: sentry-data
        {{- if and (eq .Values.filestore.backend "gcs") .Values.filestore.gcs.secretName }}
        - name: sentry-google-cloud-key
          mountPath: /var/run/secrets/google
        {{ end }}
{{- if .Values.sentry.uptimeResultsConsumer.volumeMounts }}
{{ toYaml .Values.sentry.uptimeResultsConsumer.volumeMounts | indent 8 }}
{{- end }}
        resources:
{{ toYaml .Values.sentry.uptimeResultsConsumer.resources | indent 12 }}
{{- if .Values.sentry.uptimeResultsConsumer.containerSecurityContext }}
        securityContext:
{{ toYaml .Values.sentry.uptimeResultsConsumer.containerSecurityContext | indent 12 }}
{{- end }}
{{- if .Values.sentry.uptimeResultsConsumer.sidecars }}
{{ toYaml .Values.sentry.uptimeResultsConsumer.sidecars | indent 6 }}
{{- end }}
{{- if .Values.global.sidecars }}
{{ toYaml .Values.global.sidecars | indent 6 }}
{{- end }}
      {{- if .Values.serviceAccount.enabled }}
      serviceAccountName: {{ .Values.serviceAccount.name }}-uptime-results-consumer
      {{- end }}
      volumes:
      - name: config
        configMap:
          name: {{ template "sentry.fullname" . }}-sentry
      - name: sentry-data
      {{- if and (eq .Values.filestore.backend "filesystem") .Values.filestore.filesystem.persistence.enabled (.Values.filestore.filesystem.persistence.persistentWorkers) }}
      {{- if .Values.filestore.filesystem.persistence.existingClaim }}
        persistentVolumeClaim:
          claimName: {{ .Values.filestore.filesystem.persistence.existingClaim }}
      {{- else }}
        persistentVolumeClaim:
          claimName: {{ template "sentry.fullname" . }}-data
      {{- end }}
      {{- else }}
        emptyDir: {}
      {{ end }}
      {{- if and (eq .Values.filestore.backend "gcs") .Values.filestore.gcs.secretName }}
      - name: sentry-google-cloud-key
        secret:
          secretName: {{ .Values.filestore.gcs.secretName }}
      {{ end }}
{{- if .Values.sentry.uptimeResultsConsumer.volumes }}
{{ toYaml .Values.sentry.uptimeResultsConsumer.volumes | indent 6 }}
{{- end }}
{{- if .Values.global.volumes }}
{{ toYaml .Values.global.volumes | indent 6 }}
{{- end }}
      {{- if .Values.sentry.uptimeResultsConsumer.priorityClassName }}
      priorityClassName: "{{ .Values.sentry.uptimeResultsConsumer.priorityClassName }}"
      {{- end }}
{{- end }}
