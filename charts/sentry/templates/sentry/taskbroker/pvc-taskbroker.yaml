{{- if and .Values.taskbroker.persistence.enabled (not .Values.filestore.filesystem.persistence.existingClaim) -}}
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: {{ template "sentry.fullname" . }}-taskbroker-data
  labels:
    app: sentry
    chart: "{{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}"
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
spec:
  accessModes:
    - {{ .Values.filestore.filesystem.persistence.accessMode | quote }}
  resources:
    requests:
      storage: {{ .Values.taskbroker.persistence.size | quote }}
{{- if .Values.taskbroker.persistence.storageClass }}
{{- if (eq "-" .Values.taskbroker.persistence.storageClass) }}
  storageClassName: ""
{{- else }}
  storageClassName: "{{ .Values.taskbroker.persistence.storageClass }}"
{{- end }}
{{- end }}
{{- end -}}
